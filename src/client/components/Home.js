import React, { Fragment } from 'react'

const Home = () => {
    return (
        <div>
            <div>Homeasd component</div>
            <button onClick={() => console.log('Invoke')}>Invoke</button>
        </div>
    )
}

export default Home